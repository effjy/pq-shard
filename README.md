# pq-shard
Split a master password, KEM private key, or arbitrary secret into N shares where any M reconstruct it (e.g. 3-of-5). Distribute shares across people/locations/devices; no single share leaks anything.
